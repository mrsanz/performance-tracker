version: '3.8'
services:
  performance-tracker:
    build:
      context: .
      dockerfile: infrastructure/Dockerfile
    ports:
      - "3000:3000"
    environment:
      # DATABASE_PATH: /app/data/performance-tracker.db
      PORT: 3000
      HOST: 0.0.0.0
    volumes:
      - ./data:/app/data
